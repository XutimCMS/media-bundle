{% extends '@XutimCore/admin/modalBase.html.twig' %}
{% set title = 'Edit Focal Point'|trans({}, 'admin') %}

{% block pageTitle %}
    {{ title }}
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header p-3">
            <h3 class="card-title">{{ title }}</h3>
        </div>
        <div class="card-body">
            <twig:XutimMedia:Admin:FocalPoint :media="media" :saveUrl="saveUrl" />
        </div>
    </div>
{% endblock %}
